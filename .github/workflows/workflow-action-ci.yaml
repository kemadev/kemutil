name: Workflows - CI

on:
  schedule:
    # Each day at 3:00 AM
    - cron: 0 3 * * *
  pull_request:
    types:
      - opened
      - synchronize
      - reopened
      - unlocked
    paths:
      - .github/workflows/*
      - .github/actions/*
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  workflow-lint:
    name: Workflows lint
    permissions:
      contents: read
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/kemadev/ci-cd:latest@sha256:6ce495f91dbaa89fd7915b18a76c4d095002dbce3ff2773f6897d5695ecc0de8
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.github_token }}
    steps:
      - name: Checkout
        id: checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4
      - name: Set git safe directory
        run: git config --global --add safe.directory "${GITHUB_WORKSPACE//\/home\/runner\/work\//\/__w\/}"
      - name: Workflows lint
        id: workflow-lint
        run: kema-runner gha
